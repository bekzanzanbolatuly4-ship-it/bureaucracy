# Bureaucracy Buster KZ

MVP SaaS для анализа бюрократических bottleneck в процессах (1C / Bitrix24) и расчета ROI в ₸.

## Быстрый старт

1. `docker-compose up -d`
2. Подождать миграции (backend стартует после `alembic upgrade head`)
3. `curl http://localhost:8000/health`

